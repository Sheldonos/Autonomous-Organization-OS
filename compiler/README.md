# Pocket compiler

Use `compile_pocket.py` to create a new pocket manifest and progressive-disclosure route index from the canonical substrate. Existing release pockets are immutable reference definitions; compile additions under `extensions/`.
